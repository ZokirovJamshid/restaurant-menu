import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  target: "serverless",  // Netlify uchun serverless arxitekturasi
};

export default nextConfig;
